(() => { const e = document.querySelector('.search-box'); e.addEventListener('keypress', (t => { t.keyCode === 13 && ((async function (e) { await fetch(`https://api.openweathermap.org/data/2.5/weather?q=${e}&units=metric&APPID=71a1d54910fc34b8989358aaf59ab093`).then((e => e.json())).then(n); }(e.value)), e.value = ''); })); const n = e => { document.querySelector('.location .city').innerText = `${e.name}, ${e.sys.country}`; const n = new Date(); document.querySelector('.location .date').innerText = t(n); const r = Math.round(e.main.temp); const a = (function (e) { return Math.round(1.8 * e + 32); }(r)); const c = document.querySelector('.current .temp'); c.innerHTML = `${r}°C`, c.addEventListener('click', (() => { c.innerHTML.includes('C') ? c.innerHTML = `${a}°F` : c.innerHTML.includes('F') && (c.innerHTML = `${r}°C`); })), document.querySelector('.current .weather').innerText = e.weather[0].main, document.querySelector('.hi-low').innerText = `${Math.round(e.main.temp_min)}°c / ${Math.round(e.main.temp_max)}°c`; }; const t = e => `${['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'][e.getDay()]} ${e.getDate()} ${['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'][e.getMonth()]} ${e.getFullYear()}`; })();//eslint-disable-line